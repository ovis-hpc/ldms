# This file contains environment variables for ldmsd.sampler, which will affect
# ldmsd initial configuration (e.g. transport, named socket path)

# LDMS transport option (sock, rdma, or ugni)
LDMSD_XPRT=sock
# LDMS Daemon service port
LDMSD_PORT=10000

# LDMSD configuration port, for ldmsd_controller over the network. Configuration
# transport always use 'socket', and is separated from the LDMS_XPRT.
LDMSD_CONFIG_PORT=10001
# Named socket path for ldmsd_controller
LDMSD_SOCK_PATH=@localstatedir@/run/ldmsd/sampler.sock

# Authentication file path, see @sysconfdir@/ovisauth.conf for an example
LDMSD_AUTH_FILE=@sysconfdir@/ldmsauth.conf

# LDMS plugin configuration file, see @sysconfdir@/ldmsplugin.conf for an example
LDMSD_PLUGIN_CONFIG_FILE=@sysconfdir@/ldmsplugin.sampler.conf


# These are configured by configure script, no need to change.
LDMSD_PLUGIN_LIBPATH=@LDMSD_PLUGIN_LIBPATH@
ZAP_LIBPATH=@ZAP_LIBPATH@
